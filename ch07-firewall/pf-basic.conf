# /etc/pf.conf -- worf's standard server
# Chapter 7: The Firewall That Fits on One Page

ext_if = "vtnet0"
tcp_services = "{ ssh, http, https }"

set skip on lo0
scrub in all

block in all
pass out all keep state
pass in on $ext_if proto tcp to port $tcp_services
pass in on $ext_if proto icmp
